# Copying this file is not enough: empty required values make production
# compose fail closed. Generate a real 0600 file with:
#   ./scripts/init-self-host.sh --server-url https://loca.example.com

ADMIN_TOKEN=
PUBLIC_SERVER_URL=
